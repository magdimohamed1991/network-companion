# Dashboard
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
requests==2.32.3

# ARP-spoofing bandwidth capture (scanner.py itself needs nothing beyond stdlib)
scapy==2.6.1
pysnmp==4.4.12

# Notifications (Phase 2) — Telegram just uses requests, already listed above
win11toast==0.36.3

# Speed test integration
speedtest-cli==2.1.3

# Auth (multi-user dashboard access)
bcrypt==4.2.1
python-jose==3.3.0
python-multipart==0.0.12
